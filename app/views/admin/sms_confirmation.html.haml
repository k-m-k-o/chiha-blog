.admin-index
  .admin-index__form.row
    = form_with url: sms_confirmation_admin_index_path,method: :post do |f|
      .input-field.col.s6
        = f.password_field :sms_num,id: "sms_num"
        = f.label :sms_num
      = f.submit "送信",{class: "btn admin-index__btn"}